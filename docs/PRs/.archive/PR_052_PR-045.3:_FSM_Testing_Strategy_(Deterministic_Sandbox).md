status: open

---
status: open
slice_depth: 0
---
# PR-045.3: FSM Testing Strategy (Deterministic Sandbox)

## 1. Description
Build the testing strategy outlined in Section 6 to validate the Orchestrator without invoking real LLMs.

## 2. Requirements
- Create `scripts/test_orchestrator_fsm.sh` using a temporary directory sandbox.
- Implement stub agents:
  - Stub Coder: Exit 0 or 1 based on test scenario.
  - Stub Reviewer: Output `[ACTION_REQUIRED]` or `[LGTM]`.
  - Stub Arbitrator: Output `[OVERRIDE_LGTM]` or `[CONFIRM_REJECT]`.
  - Stub Planner: Simulate file generation for slicing.
- Assert git branch state transitions and PR YAML status mutations (`closed`, `superseded`, `blocked_fatal`).
