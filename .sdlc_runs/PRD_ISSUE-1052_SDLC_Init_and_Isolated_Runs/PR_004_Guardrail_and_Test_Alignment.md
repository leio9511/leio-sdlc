status: open

# PR-4: Guardrail and Test Alignment

## 1. Objective
Update the `.sdlc_guardrail` and all E2E tests to align with the new `.sdlc_runs/` directory structure for runtime artifacts.

## 2. Scope (Functional & Implementation Freedom)
- Modify the `.sdlc_guardrail` file to protect the `.sdlc_runs/` directory from malicious Coder modifications, instead of the root-level `Review_Report.md`.
- If the guardrail is generated by a script (e.g., `patch_guardrails.sh`), update the script as well.
- Update all associated E2E bash tests (`scripts/test_*.sh`) and python tests to mock and assert artifacts in their new `.sdlc_runs/` locations.
*The Coder MUST search the workspace, understand the existing code structure, and autonomously decide which files to create or modify to implement this logic.*

## 3. TDD & Acceptance Criteria
- The `.sdlc_guardrail` must effectively prevent unauthorized modifications to the `.sdlc_runs/` directory.
- All E2E tests must pass successfully, correctly mocking and asserting artifacts in their new locations.
- The full E2E test suite (`bash scripts/test_sdlc_cujs.sh` or equivalent) must pass.
- The Coder MUST write or update tests for this specific functional slice. All tests MUST pass (GREEN) before submitting.
