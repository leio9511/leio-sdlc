status: open

# PR-3: Configuration and Prompt Updates

## 1. Objective
Update `config/prompts.json` and `scripts/handoff_prompter.py` to ensure all references to `Review_Report.md` reflect its new dynamic, nested location within `.sdlc_runs/<PR_Name>/`.

## 2. Scope (Functional & Implementation Freedom)
- Modify `config/prompts.json` to replace static references to `Review_Report.md` with a dynamic placeholder or a mechanism to construct the full path.
- Modify `scripts/handoff_prompter.py` to correctly resolve the path to `Review_Report.md` within the appropriate `.sdlc_runs/<PR_Name>/` directory.
*The Coder MUST search the workspace, understand the existing code structure, and autonomously decide which files to create or modify to implement this logic.*

## 3. TDD & Acceptance Criteria
- All prompts generated by `scripts/handoff_prompter.py` must contain the correct, full path to `Review_Report.md`.
- The system continues to function correctly with the updated prompt configurations.
- The Coder MUST write or update tests for this specific functional slice. All tests MUST pass (GREEN) before submitting.
