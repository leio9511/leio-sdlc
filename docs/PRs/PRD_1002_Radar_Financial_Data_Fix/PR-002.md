status: closed

# PR-002: Merge Fundamental Data and Remove Hardcoded PE Stub

## 1. Objective
Remove the malicious hardcoded PE stub (`df_a["市盈率-动态"] = 15.0`) from the radar pipeline and integrate the new `fetch_fundamental_data()` to enrich the QMT tick data with authentic financial metrics.

## 2. Scope & Implementation Details
- Target Project: `/root/.openclaw/workspace/AMS`
- `pilot_stock_radar.py` & `adapter.py`: Remove `df_a["市盈率-动态"] = 15.0` completely.
- `pilot_stock_radar.py`: Import and call `fetch_fundamental_data()`. Use `pandas.merge()` to join the real-time QMT DataFrame with the fundamental DataFrame on the `代码` column before the radar's valuation filtering logic executes.
- `tests/test_data_source.py`: Add a test that simulates the full merge process (mocking QMT tick and fundamental data) and asserts that the final DataFrame passed to the radar correctly mapped the PE and Market Cap values without falling back to hardcoded numbers.

## 3. TDD & Acceptance Criteria
- Test Script: `tests/test_data_source.py`
- The test must assert that the merged DataFrame has `市盈率-动态` and `总市值` columns derived from the fundamental data, and that `15.0` is not hardcoded for all rows.
- The radar script `pilot_stock_radar.py` must execute without errors and output a filtered list using the real PE metrics.

> [Escalation] Tier 1 Reset triggered due to Coder failure or Arbitrator rejection.
